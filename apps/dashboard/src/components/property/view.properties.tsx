import { Property } from "@/features/property/types"
import { useQuery } from "@tanstack/react-query"

const ViewProperties = ({ properties }: { properties: Property[] }) => {

    // const { data } = useQuery({

    // })

    return (
        <></>
    )
}