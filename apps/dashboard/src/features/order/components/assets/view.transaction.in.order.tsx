import { Transaction } from "@/features/transaction/types"

interface ViewTransactionInOrderProps {
    transactions: Transaction[]
}

export const ViewTransactionInOrder = ({ transactions }: ViewTransactionInOrderProps) => {
    return (
        <></>
    )
}