services:
  business:
    build: 
      context: .
      dockerfile: apps/business/Dockerfile
    ports:
    - "7010:3000"
    environment:
      NEXT_PUBLIC_URL_BACK_END: http://server:3333

  dashboard: 
    build: 
      context: . 
      dockerfile: apps/dashboard/Dockerfile 
      args: 
        BACKEND_URL: ${BACKEND_URL} 
        NEXT_INTERNAL_BACKEND_URL: ${NEXT_INTERNAL_BACKEND_URL} 
    ports: 
      - "7011:3000" 
    environment: 
      BACKEND_URL: ${BACKEND_URL} 
      NEXT_INTERNAL_BACKEND_URL: ${NEXT_INTERNAL_BACKEND_URL}

  server_old:
    build:
      context: .
      dockerfile: apps/server.old/Dockerfile
    ports:
      - "3333:3333"
    environment:
      - PORT=${PORT}
      - REGION_MINIO=${REGION_MINIO}
      - URL_MINIO=${URL_MINIO}
      - URL_PUBLIC_MINIO=${URL_PUBLIC_MINIO}
      - ACCESS_KEY_MINIO=${ACCESS_KEY_MINIO}
      - SECRET_KEY_MINIO=${SECRET_KEY_MINIO}
      - URL_MONGO=${URL_MONGO}
      - GOOGLE_KEY=${GOOGLE_KEY}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - URL_FRONT=${URL_FRONT}
      - KEY_TOKEN=${KEY_TOKEN}
      - DATABASE_URL=${DATABASE_URL}
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      - BETTER_AUTH_URL=${BETTER_AUTH_URL} 
      - BETTER_AUTH_URI=${BETTER_AUTH_URI}
      - RESEND_API_KEY=${RESEND_API_KEY}